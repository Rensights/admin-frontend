# Default values for admin-frontend
replicaCount: 1

image:
  repository: ghcr.io/rensights/admin-frontend
  tag: ""  # Will be set by CI/CD with pipeline run number
  pullPolicy: IfNotPresent

imagePullSecrets: []

service:
  type: ClusterIP
  port: 3001

ingress:
  enabled: false
  className: "kong"
  annotations: {}
  hosts:
    - host: example.com
      paths:
        - path: /
          pathType: Prefix

resources:
  requests:
    cpu: "100m"
    memory: "256Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"

livenessProbe:
  httpGet:
    path: /api/health
    port: http
  initialDelaySeconds: 90
  periodSeconds: 30
  timeoutSeconds: 10
  failureThreshold: 3

readinessProbe:
  httpGet:
    path: /api/health
    port: http
  initialDelaySeconds: 60
  periodSeconds: 10
  timeoutSeconds: 10
  failureThreshold: 5

extraEnv: {}

secrets:
  # Single secret holding ALL app config for every app. Managed in Portainer.
  # Admin frontend reads: ADMIN_API_URL, ADMIN_MAIN_BACKEND_URL.
  sharedConfig: dev-shared-config

serviceAccount:
  create: true
  annotations: {}
  name: ""

networkPolicy:
  enabled: true
  # Kong namespace labels to match
  # Default matches namespace with label "name: kong"
  kongNamespaceLabels:
    name: kong









